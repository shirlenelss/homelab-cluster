apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: linkding-data
  app: linkding
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard   # or gp2, gp3, local-path, etc.
