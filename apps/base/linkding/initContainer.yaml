initContainers:
- name: volume-fix
  image: alpine
  securityContext:
    runAsUser: 0        # root
  command: ["sh", "-c", "chown -R 33:33 /etc/linkding/data && chmod -R g+rwX /etc/linkding/data"]
  volumeMounts:
  - mountPath: /etc/linkding/data
    name: linkding-data